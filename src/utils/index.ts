import sleepMethod from './sleep'
import executeShellCommandMethod from './execute-shell-command'

export const sleep = sleepMethod
export const executeShellCommand = executeShellCommandMethod

export default {
	sleep,
	executeShellCommand,
}
